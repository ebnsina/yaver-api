// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: chatsettings.sql

package gen

import (
	"context"
)

const getChatSettings = `-- name: GetChatSettings :one
SELECT instructions, widget_title, welcome, accent
FROM chat_settings WHERE org_id = $1
`

type GetChatSettingsRow struct {
	Instructions string
	WidgetTitle  string
	Welcome      string
	Accent       string
}

func (q *Queries) GetChatSettings(ctx context.Context, orgID string) (GetChatSettingsRow, error) {
	row := q.db.QueryRow(ctx, getChatSettings, orgID)
	var i GetChatSettingsRow
	err := row.Scan(
		&i.Instructions,
		&i.WidgetTitle,
		&i.Welcome,
		&i.Accent,
	)
	return i, err
}

const upsertChatSettings = `-- name: UpsertChatSettings :exec
INSERT INTO chat_settings (org_id, instructions, widget_title, welcome, accent, updated_at)
VALUES ($1, $2, $3, $4, $5, now())
ON CONFLICT (org_id) DO UPDATE
SET instructions = EXCLUDED.instructions,
    widget_title = EXCLUDED.widget_title,
    welcome      = EXCLUDED.welcome,
    accent       = EXCLUDED.accent,
    updated_at   = now()
`

type UpsertChatSettingsParams struct {
	OrgID        string
	Instructions string
	WidgetTitle  string
	Welcome      string
	Accent       string
}

func (q *Queries) UpsertChatSettings(ctx context.Context, arg UpsertChatSettingsParams) error {
	_, err := q.db.Exec(ctx, upsertChatSettings,
		arg.OrgID,
		arg.Instructions,
		arg.WidgetTitle,
		arg.Welcome,
		arg.Accent,
	)
	return err
}
