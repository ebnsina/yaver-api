# Yaver API — required environment. Copy to .env for local dev (.env is gitignored).
# All vars are required; the app fails to boot if any is missing (no hardcoded defaults).

YAVER_ENV=dev
YAVER_PORT=8080
